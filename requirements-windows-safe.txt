# ===================================================================
# Windows環境で安全にインストール可能なパッケージリスト
# ===================================================================
# 使用方法: pip install -r requirements-windows-safe.txt
# 
# このファイルは以下の特徴があります:
# - プリビルド版が確実に存在するパッケージのみ
# - バージョン固定を避けて互換性を重視
# - コンパイラを必要としないパッケージのみ
# ===================================================================

# コアWebフレームワーク（確実にプリビルド版あり）
fastapi>=0.104.0
uvicorn>=0.24.0
sqlalchemy>=2.0.23
alembic>=1.13.0
python-multipart>=0.0.6
requests>=2.31.0

# データベース（プリビルド版あり）
psycopg-binary>=3.2.0

# 基本的な開発ツール
httpx>=0.24.1
pytest>=7.4.3
pytest-asyncio>=0.21.1

# ビルドツール（プリビルド版あり）
pyinstaller>=6.0.0

# 認証・セキュリティ（オプション）
# python-jose[cryptography]>=3.3.0  # 暗号化ライブラリ必要
# passlib[bcrypt]>=1.7.4             # bcryptコンパイル必要

# 外部サービス（オプション、プリビルド版あり）
# boto3>=1.34.0
# celery>=5.3.4  
# redis>=5.0.1
# openai>=1.3.8

# ===================================================================
# 以下のパッケージは個別インストールを推奨:
# 
# pip install scikit-learn==1.7.0 --only-binary=all
# pip install pandas --only-binary=all  
# pip install streamlit --only-binary=all
# pip install flet --only-binary=all
# pip install pyinstaller
# ===================================================================